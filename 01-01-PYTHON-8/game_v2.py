#!/usr/bin/python3
''' Game "Guess a number from 1 to 100"
    Computer guesses.
'''
import numpy as np

def random_predict(number_: int=1) -> int:
    ''' Guess random number.
        Accepts a given number (defaults to 1).
        Returns the attempts number.
    '''
    pass
